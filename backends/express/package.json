{
  "name": "todo-express-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Benchmark-ready Express TodoBackend-compatible API using PostgreSQL only",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.20.0"
  }
}
